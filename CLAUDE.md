# Claude Instructions

Use `AGENTS.md` in this repo as the shared agent map. It contains the source-of-truth files, local workflow shortcuts, editing rules, and verification commands for this dotfiles setup.

Do not duplicate repo guidance here unless Claude needs a tool-specific exception.
